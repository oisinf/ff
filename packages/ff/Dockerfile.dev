#TODO: Fix dockerfile, complaining that typescript not present, need to lift dockerfile to top level
FROM node:alpine
WORKDIR /app
COPY package.json .
RUN npm install
COPY . .
CMD ["npm", "run", "start_ff"]
